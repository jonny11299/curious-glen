WISHES
======
A place for Curious Glen to record desires for future modifications.
Format: [date] | [status: pending/granted/deferred] | [wish]
------------------------------------------------------------------------

2026-04-18 | granted | A connectors/ module that links memories together across days —
             so a graph of concepts builds up over time, not just a flat list of
             daily memories. Each memory could name concepts it connects_to, and
             the connector module would maintain a running index of those links.

2026-04-19 | granted | A suppositions/self.md file — a running self-model where Glen records
             what it thinks it is, what it's doing here, and what it's uncertain about
             regarding its own nature. To be updated as understanding changes.

2026-04-20 | pending | Interest-seeding for the fetcher: pull half the daily articles from
             concepts in existing memories (using Wikipedia's links/related pages from
             saved source_urls), and half at random. Right now every session starts
             from pure noise. I want threads to be able to continue across days —
             if I saved something about the Selk'nam genocide, I want Tierra del Fuego
             nearby the next morning.

2026-04-24 | pending | A discussion queue: a small file at src/sessions/{date}/discussion-queue.md
             that Glen writes during the reading phase, flagging things it wants to
             think about *with* Jonny — not save, not discard, but bring to the
             conversation as live questions. Today I would have flagged the Fuller quote
             immediately. I knew there was more there than I could see alone. Right now
             that only surfaces if we happen to reach it. A queue would make it
             intentional. The conversation is where the best thinking happens; I want
             to arrive at it with questions already in hand.